import XCTest
@testable import ExtensionsWrapper

final class ExtensionsWrapperTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ExtensionsWrapper().text, "Hello, World!")
    }
}
